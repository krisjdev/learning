// if condition to check if age is not between 14 and 90 inclusive
if (!(age >= 14 && age <= 90)) {
    alert("age not between range");
}

// without NOT operator
if (age < 14 || age > 90) {
    alert("age not between range");
}